const points = require("./data");
const level = require("./data");
const achievments = require("./data");
const object = require("./data");

function log() {
  console.log(points);
  console.log(level);
  console.log(achievments);
  console.log(object);
}
log();
